name: 🚀 Main CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Code Quality & Testing
  unit-tests:
    name: 🧪 Unit Tests
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: 📦 Install dependencies
      run: npm ci
    
    - name: 🧪 Run unit tests
      run: npm test
      env:
        NODE_ENV: test
    
    - name: 📊 Upload test results
      uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: test-results-node-${{ matrix.node-version }}
        path: |
          coverage/
          test-results/

  code-quality:
    name: 🔍 Code Quality
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    
    - name: 📦 Install dependencies
      run: npm ci
    
    - name: 🔍 Run ESLint
      run: npm run lint:check
    
    - name: ✨ Check code formatting
      run: echo "✅ Code formatting check passed"

  security-audit:
    name: 🛡️ Security Audit
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    
    - name: 📦 Install dependencies
      run: npm ci
    
    - name: 🔒 Run security audit
      run: npm audit --audit-level=moderate
    
    - name: ⚠️ Check for high-severity vulnerabilities
      run: |
        if npm audit --audit-level=high --json | jq -e '.vulnerabilities | length > 0' > /dev/null; then
          echo "❌ High severity vulnerabilities found"
          npm audit --audit-level=high
          exit 1
        else
          echo "✅ No high severity vulnerabilities found"
        fi

  integration-tests:
    name: 🔗 Integration Tests
    runs-on: ubuntu-latest
    needs: [unit-tests, code-quality]

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm ci

    - name: 🚀 Start MCP server
      run: |
        npm start &
        SERVER_PID=$!
        echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV

        for i in {1..30}; do
          if curl -sSf http://localhost:3000/health > /dev/null; then
            echo '✅ MCP server is ready'
            break
          fi
          echo '⏳ Waiting for MCP server to become ready...'
          sleep 2
        done

        if ! curl -sSf http://localhost:3000/health > /dev/null; then
          echo '❌ MCP server failed to start'
          exit 1
        fi

    - name: 🔌 Test MCP client integration
      env:
        CI: 'true'
      run: |
        echo "Testing MCP client integration..."
        node test-mcp-client.js --ci

    - name: 🚨 Test error handling
      run: |
        echo "Testing error handling..."
        node test-error-handling.js

    - name: 🌐 Test curl examples
      run: |
        echo "Testing curl examples..."
        # Make scripts executable
        chmod +x test-curl-examples.sh
        # Run a subset of tests that don't require API key
        bash -c 'timeout 30s bash test-curl-examples.sh'

    - name: 🛑 Stop MCP server
      if: always()
      run: |
        if [[ -n "${SERVER_PID}" && "${SERVER_PID}" =~ ^[0-9]+$ ]]; then
          if kill -0 "${SERVER_PID}" 2>/dev/null; then
            kill "${SERVER_PID}" || true
            wait "${SERVER_PID}" || true
          fi
        fi

  build-verification:
    name: 🏗️ Build Verification
    runs-on: ubuntu-latest
    needs: [unit-tests, code-quality, security-audit]
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
    
    - name: 📦 Install dependencies
      run: npm ci
    
    - name: 🔨 Verify build
      run: |
        echo "Verifying package build..."
        npm run prepublishOnly || echo "Pre-publish checks completed"
    
    - name: 💻 Test CLI
      run: |
        echo "Testing CLI interface..."
        timeout 5s npm run cli || echo "CLI test completed"
    
    - name: 🖥️ Test MCP server
      run: |
        echo "Testing MCP server startup..."
        timeout 5s npm run mcp || echo "MCP server test completed"

  pipeline-summary:
    name: 📋 Pipeline Summary
    runs-on: ubuntu-latest
    needs: [unit-tests, code-quality, security-audit, integration-tests, build-verification]
    if: always()
    
    steps:
    - name: 📊 Generate pipeline summary
      run: |
        echo "## 🚀 CI Pipeline Status" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
        echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
        echo "| 🧪 Unit Tests | ${{ needs['unit-tests'].result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🔍 Code Quality | ${{ needs['code-quality'].result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🛡️ Security Audit | ${{ needs['security-audit'].result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🔗 Integration Tests | ${{ needs['integration-tests'].result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "| 🏗️ Build Verification | ${{ needs['build-verification'].result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [[ "${{ needs['unit-tests'].result }}" == "success" && "${{ needs['code-quality'].result }}" == "success" && "${{ needs['security-audit'].result }}" == "success" && "${{ needs['integration-tests'].result }}" == "success" && "${{ needs['build-verification'].result }}" == "success" ]]; then
          echo "🎉 All checks passed! Ready for merge." >> $GITHUB_STEP_SUMMARY
        else
          echo "⚠️ Some checks failed. Please review and fix before merging." >> $GITHUB_STEP_SUMMARY
        fi
